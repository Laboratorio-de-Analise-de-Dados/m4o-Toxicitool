1. Desvio Padrão (Sinal vs. Ruído)
  Mesmo tendo mais dados, a variabilidade interna de mouse_vo e mouse_ip é significativamente menor do que a de
  tasks menores como rat_vi. 
   * Mouse VO: std = 0.58
   * Mouse IP: std = 0.63
   * Rat VI: std = 0.88 (a task que performa melhor, R²=0.57)

  Quando o desvio padrão é baixo, o modelo tem dificuldade em "distinguir" as moléculas, pois os valores de
  toxicidade estão muito aglomerados (alta densidade em uma faixa estreita). Qualquer ruído experimental nos dados
  brutos acaba dominando o sinal.

  2. O Efeito "Scaffold Split"
  Você está usando o split por Scaffold, que é o mais rigoroso possível.
   * Nas tarefas com muitos dados (mouse_vo e mouse_ip), é provável que existam scaffolds muito frequentes no
     treino que não aparecem no teste. 
   * O modelo "aprende" bem os scaffolds populares, mas o Pearson cai drasticamente quando ele tenta generalizar
     para scaffolds novos (que é exatamente o que o split de scaffold testa).

  3. Correlação de Pearson vs R²
  Pelos logs:
   * mouse_ip tem R² = 0.21.
   * rat_vi tem R² = 0.57.

  Isso confirma que o modelo está sofrendo para encontrar padrões lineares nas tasks de mouse. 

  Sugestão de teste:
  Para confirmar se o problema é a complexidade química ou o split, poderíamos rodar um experimento rápido com
  Random Split. Se o Pearson subir para > 0.7 no Random Split, significa que os dados são bons, mas o modelo
  simplesmente não consegue extrapolar para famílias químicas novas (scaffolds diferentes).

   > então eu ja estou aplicando? não tem nada a se adicionar?                                 
▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀▀
✦ Sim, você já está aplicando essa lógica híbrida no seu pipeline atual. No entanto, sempre
  há espaço para refinamento estatístico se você quiser extrair o máximo de performance.

  Aqui está o que já está funcionando e o que você poderia adicionar para tornar o artigo
  ainda mais robusto:

  1. O que já está "no código" (Etapa 2 e 3):
  No seu script src/representation/Representacao.py e na configuração do config.py:
   * Concatenação: O código pega os 2048 bits do Morgan Fingerprint e "gruda" os descritores
     físico-químicos no final do vetor.
   * Escalonamento (Scaling): Como o Peso Molecular (ex: 400) é muito maior que um bit do
     Fingerprint (0 ou 1), o seu código já aplica um StandardScaler. Isso é vital para que o
     modelo não ignore os bits por causa da diferença de magnitude.

  2. O que você PODE adicionar (Oportunidades de Melhoria):

  Embora a lógica esteja lá, você poderia considerar dois ajustes finos que costumam subir o
  R²:

  A. Seleção de Descritores (Feature Selection)
  Atualmente você usa "lista": "todos". Isso calcula cerca de 200 descritores do RDKit.
   * O Problema: Muitos descritores são altamente correlacionados (ex: MolWt e
     HeavyAtomCount). Isso gera redundância e pode confundir a rede neural.
   * A Sugestão: Adicionar um filtro de Correlação de Spearman ou VIF (Variance Inflation
     Factor) para manter apenas os descritores que trazem informação nova.

  B. Inclusão de Grupos Funcionais Específicos como Features
  O diagnóstico que acabamos de rodar mostrou que você tem 81% de anéis aromáticos e 25% de
  anilinas.
   * A Sugestão: No Representacao.py, além dos descritores contínuos (LogP, MW), você poderia
     injetar esses contadores de grupos funcionais (fr_aniline, fr_nitro, etc.) como features
     extras. O Fingerprint às vezes "perde" a contagem exata desses grupos devido a colisões
     de bits (hashing). Ter o número exato de grupos Nitro, por exemplo, é um sinal preditor
     de toxicidade muito forte.

  C. Análise de Importância de Features (SHAP)
  Para o seu artigo, seria fantástico adicionar uma etapa de SHAP Values.
   * Isso permitiria você dizer: "O modelo MTL previu que esta molécula é tóxica
     principalmente devido ao seu alto LogP e à presença de um grupo Anilina". Isso
     transforma a "caixa-preta" do Deep Learning em algo explicável quimicamente.

  ---

  Resumo:
  Você já tem o motor (Fingerprints + Descritores). O que falta, se você quiser ir para uma
  revista de alto impacto, é a ajustagem fina (limpeza de descritores redundantes) e a
  interpretabilidade (entender o que o modelo está priorizando).

  Quer que eu verifique no código da Representacao.py como está a filtragem desses
  descritores hoje para vermos se podemos otimizar?